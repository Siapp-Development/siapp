node_modules/
dist/
coverage/
*.tsbuildinfo
*.local
.env
.env.*
.DS_Store
logs/
*.log

# keep the example env committed
!.env.example
# keep the web app's baseline env committed — Firebase web config values are
# public client identifiers, not secrets (see apps/web/.env header comment)
!apps/web/.env

# pnpm (replace npm lock file)
package-lock.json

# turbo
.turbo/
.firebase/

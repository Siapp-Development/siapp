node_modules/
dist/
coverage/
*.tsbuildinfo
*.local
.env
.env.*
.DS_Store
logs/
*.log

# keep the example env committed
!.env.example
# functions deploy-time params (public values, no secrets) — CI deploys are
# non-interactive and can't prompt for them (#57)
!backend/functions/.env.siapp-prod
# keep the web app's baseline env committed — Firebase web config values are
# public client identifiers, not secrets (see apps/web/.env header comment)
!apps/web/.env

# pnpm (replace npm lock file)
package-lock.json

# turbo
.turbo/
.firebase/
